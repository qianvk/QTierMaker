#pragma once

#include <vkui/window/VkMessageDialog.h>

namespace qtm {

/** Application-facing name for VkUI's platform-ordered native prompt. */
using AppMessageDialog = vkui::VkMessageDialog;

} // namespace qtm
